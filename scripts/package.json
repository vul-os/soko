{
  "name": "soko-site-checks",
  "private": true,
  "type": "module",
  "description": "Playwright render gate for site/index.html and site/docs.html. Separate from the repo-root package.json, which carries the puppeteer-core that tools/diagrams.mjs uses — these are different jobs with different browsers and neither should drag in the other.",
  "dependencies": {
    "playwright": "^1.44.0"
  },
  "scripts": {
    "check": "node check-render.mjs",
    "check:selftest": "node check-render.mjs --selftest"
  }
}
